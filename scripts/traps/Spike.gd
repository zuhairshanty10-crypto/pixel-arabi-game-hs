extends TrapBase

# Spike trap - inherits everything from TrapBase.
# No extra code needed since TrapBase handles collision and killing implicitly.
# We just need this script attached to the node to identify it easily.

func _trap_init() -> void:
	# Add any spike-specific initialization here if needed later
	pass
